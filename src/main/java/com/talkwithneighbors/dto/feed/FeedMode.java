package com.talkwithneighbors.dto.feed;

/**
 * Supported feed ordering modes. The enum names are part of the HTTP query contract.
 */
public enum FeedMode {
    RECOMMENDED,
    NEARBY,
    LATEST
}
